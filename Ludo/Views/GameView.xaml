<UserControl x:Class="Ludo.Views.GameView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:Ludo.Views"
             xmlns:vm="clr-namespace:LudoClient.ViewModels.InGameViewModels"
             mc:Ignorable="d"
             MinWidth="400" MinHeight="400"
             Loaded="UserControl_Loaded">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </UserControl.Resources>


    <Grid Margin="0,0,0,0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="613*"/>
            <ColumnDefinition Width="187*"/>
        </Grid.ColumnDefinitions>

        <!-- Hele brættet -->
        <ItemsControl ItemsSource="{Binding BoardCells}" Grid.ColumnSpan="2" Margin="0,26,0,0">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Rows="15" Columns="15"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            

            
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border Background="{Binding CellColor}" BorderBrush="Black" BorderThickness="0.1">
                        <Grid>

                            <!-- Trigger stjerne hvis IsStar er true-->
                                <TextBlock
                                    Margin="0,-7,0,0"
                                Text="★"
                                FontSize="50"
                                Foreground="BlanchedAlmond"
                                HorizontalAlignment="Center"
                                VerticalAlignment="center"
                                Visibility="{Binding IsStar, Converter={StaticResource BoolToVisibilityConverter}}"/>
       
                            <!-- Trigger globe hvis IsGlobe er true-->
                            <TextBlock 
                                Margin="0,-3,0,0"
                                Text="🌐"
                                FontSize="40"
                                Foreground="{Binding GlobeColor}"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                Visibility="{Binding IsGlobe, Converter={StaticResource BoolToVisibilityConverter}}"/>      


                            <!-- Brikker -->
                            <ItemsControl ItemsSource="{Binding Pieces}" AlternationCount="64" Panel.ZIndex="1">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <Grid/>
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>

                                <ItemsControl.ItemContainerStyle>
                                    <Style TargetType="ContentPresenter">
                                        <Setter Property="Panel.ZIndex"
                                                Value="{Binding RelativeSource={RelativeSource Self}, Path=(ItemsControl.AlternationIndex)}"/>
                                    </Style>
                                </ItemsControl.ItemContainerStyle>

                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
                                            <Button
                                                Command="{Binding DataContext.SelectPieceCommand,
                                                                  RelativeSource={RelativeSource AncestorType={x:Type UserControl}}}"
                                                CommandParameter="{Binding .}"
                                                Background="Transparent"
                                                BorderBrush="{x:Null}"
                                                Padding="0"
                                                Focusable="False">
                                                <Ellipse Width="{Binding VisualSize}" Height="{Binding VisualSize}"
                                                         Fill="{Binding Fill}" Stroke="Black" StrokeThickness="1"/>
                                            </Button>
                                        </Grid>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <!-- Exit game button -->
        <Button x:Name="ExitBtn"
                Content="Exit Game"
                HorizontalAlignment="Left"
                Height="48"
                Margin="2,2,0,0"
                VerticalAlignment="Top"
                Width="80"
                Command="{Binding ExitGameCommand}"/>

        <!-- Steps input -->
        <StackPanel Orientation="Horizontal"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Top"
                    Margin="100,2,0,0">
            <TextBlock Text="Steps:" Margin="0,6,6,0"/>
            <TextBox Width="60" Text="{Binding MoveSteps, UpdateSourceTrigger=PropertyChanged}"/>
        </StackPanel>
    </Grid>
</UserControl>