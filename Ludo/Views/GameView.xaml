<UserControl x:Class="Ludo.Views.GameView"
             
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Ludo.Views"
        xmlns:vm="clr-namespace:LudoClient.ViewModels.InGameViewModels"
        mc:Ignorable="d"
        Height="800" Width="800" Loaded="UserControl_Loaded">



    <Grid Margin="0,111,0,0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="613*"/>
            <ColumnDefinition Width="187*"/>
        </Grid.ColumnDefinitions>
        <ItemsControl ItemsSource="{Binding BoardCells}" Grid.ColumnSpan="2" Margin="0,26,0,0">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Rows="15" Columns="15"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>

            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <!-- Celles baggrund -->
                        <Border Background="{Binding CellColor}"
                                BorderBrush="Black"
                                BorderThickness="0.5" />


                        <!-- Brikker i denne celle -->
                        <ItemsControl ItemsSource="{Binding Pieces}">

                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Canvas >

                                        <Ellipse

                                            Width="{Binding Size}" Height="{Binding Size}"        
                                                 Fill="{Binding Fill}"
                                                 Stroke="{Binding Stroke}"
                                                 StrokeThickness="{Binding StrokeThickness}"
                                                 Margin="1"
                                            Canvas.Left="{Binding PositionX}"
                                            Canvas.Top="{Binding PositionY}">


                                        </Ellipse>

                                    </Canvas>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>

                        </ItemsControl>


                    </Grid>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
        <Button x:Name="SaveGameBtn" Content="Save Game" HorizontalAlignment="Left" Height="48" Margin="2,2,0,0" VerticalAlignment="Top" Width="80" Command="{Binding SaveGameCommand}"/>

        <StackPanel Margin="130,-82,130,698" Grid.ColumnSpan="2">
            <Button Content="Send Ping" Command="{Binding PingCommand}" Width="100" Height="30"/>
            <Label Content="{Binding PingText}" FontSize="16" Margin="0,10,0,0"/>
        </StackPanel>
    </Grid>

</UserControl>
